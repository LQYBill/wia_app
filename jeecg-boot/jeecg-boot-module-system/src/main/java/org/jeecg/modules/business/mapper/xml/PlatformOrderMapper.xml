<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.business.mapper.PlatformOrderMapper">

    <select id="pagePendingOrderByClientId" resultType="org.jeecg.modules.business.vo.clientPlatformOrder.ClientPlatformOrderPage">
        SELECT p.*
        FROM platform_order p
                 JOIN shop s ON p.shop_id = s.id
                 JOIN client c ON s.owner_id = c.id
        WHERE c.id = #{clientId}
          AND p.status = 2
        LIMIT #{offset}, #{size}
    </select>

    <select id="countTotal" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM platform_order p
                 JOIN shop s ON p.shop_id = s.id
                 JOIN client c ON s.owner_id = c.id
        WHERE c.id = #{clientId}
    </select>

    <update id="updateStatus">
        UPDATE platform_order
        SET status = #{status}
        WHERE id = #{orderID}
    </update>

    <update id="batchUpdateStatus">
        UPDATE platform_order
        SET status = #{status}
        WHERE id IN
        <foreach collection="orderIDList" item="item" index="index"
                 close=")" open="(" separator=",">
            #{item}
        </foreach>
    </update>

</mapper>
